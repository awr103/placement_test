<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Result - Placement Test System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h2>Test Result</h2>
        
        <% if(typeof messages !== 'undefined' && messages.success_msg) { %>
            <div class="alert alert-success"><%= messages.success_msg %></div>
        <% } %>
        <% if(typeof messages !== 'undefined' && messages.error_msg) { %>
            <div class="alert alert-danger"><%= messages.error_msg %></div>
        <% } %>

        <div class="card">
            <div class="card-header">
                <h4>Student: <%= test.student_name %></h4>
                <p class="mb-0">Program: <%= test.program %></p>
                <p>Completed on: <%= new Date(test.completed_datetime).toLocaleString() %></p>
            </div>
            <div class="card-body">
                <div class="row mb-4">
                    <div class="col-md-12">
                        <h5>Recommended Program & Level</h5>
                        <p class="lead"><%= test.recommended_program || 'Not specified' %></p>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <h5>Grammar Notes</h5>
                        <p><%= test.grammar_notes %></p>
                    </div>
                    <div class="col-md-6 mb-3">
                        <h5>Vocabulary Notes</h5>
                        <p><%= test.vocabulary_notes %></p>
                    </div>
                    <div class="col-md-6 mb-3">
                        <h5>Fluency Notes</h5>
                        <p><%= test.fluency_notes %></p>
                    </div>
                    <div class="col-md-6 mb-3">
                        <h5>Pronunciation Notes</h5>
                        <p><%= test.pronunciation_notes %></p>
                    </div>
                </div>

                <div class="row">
                    <div class="col-12">
                        <h5>Additional Comments</h5>
                        <p><%= test.teacher_comments || 'No additional comments' %></p>
                    </div>
                </div>

                <div class="mt-4">
                    <a href="/sales/dashboard" class="btn btn-secondary">Back to Dashboard</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html> 